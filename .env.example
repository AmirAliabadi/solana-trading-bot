# Your Solana Wallet Private Key (Base58 encoded string)
PRIVATE_KEY=your_base58_private_key_here

# The RPC URL to use for sending transactions (Mainnet Beta recommended, or your own Helius/QuickNode RPC)
RPC_URL=https://api.mainnet-beta.solana.com

# Choose your trading strategy. Available options:
# MEAN_REVERSION, ALWAYS_BUY, BOLLINGER_BANDS, TREND_FOLLOWING, 
# SIMPLE_TREND, VOLUME_BREAKOUT, GRID_SCALPER, DYNAMIC_TRAILING, 
# SIMPLE_PERCENT
ACTIVE_STRATEGY=SIMPLE_TREND

# Name of the active configuration profile inside strategies/configs/ (leave blank to use <ACTIVE_STRATEGY>-default.json)
ACTIVE_STRATEGY_CONFIG=SIMPLE_TREND-default.json

# Jupiter Public API for quotes
JUPITER_API_URL=https://public.jupiterapi.com

# Amount of SOL to trade in each run (Default is 0.01)
TRADE_AMOUNT_SOL=0.01

# Whether to skip preflight checks when sending transactions
SKIP_PREFLIGHT=true

# How often the bot polls for market data (in milliseconds)
POLL_INTERVAL=60000

# Live Market Polling Interval (Valid Options: 1m, 3m, 5m, 15m, 30m, 1h, 2h, 4h, 6h, 8h, 12h, 1d)
BINANCE_INTERVAL=15m

# Liquidity/Price Impact Guard (%)
# If a trade will cause more than this percentage of price impact, it will be blocked.
MAX_PRICE_IMPACT=0.1

# Data Logging for Backtesting
# If true, the bot will save raw TA data to logs/backtest_data.csv
ENABLE_DATA_LOGGING=true

# Post-Swap Simulation Delay (ms) - How long to wait after a swap before resuming polling
POST_SWAP_DELAY_MS=5000

# Profit Threshold (%) - Minimum profit required to swap back to starting token (0 to disable)
PROFIT_THRESHOLD_PERCENT=0.2

# Discord Notifications
# Get your webhook URL from Discord: Server Settings > Integrations > Webhooks
DISCORD_WEBHOOK_URL=your_discord_webhook_url_here

# Heartbeat Interval (ms) - How often to send a status update to Discord (default: 3600000 = 1 hour)
# Set to a lower value during testing, e.g. 300000 = 5 minutes
HEARTBEAT_INTERVAL_MS=3600000

REQUIRE_BUY_PROFIT=true